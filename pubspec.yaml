name: async_controller
description: A library for managing asynchronously loaded data in Flutter. 
version: 0.0.1
author: szotp
homepage: https://github.com/szotp/async_controller

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  connectivity: ^0.4.3

dev_dependencies:
  flutter_test:
    sdk: flutter