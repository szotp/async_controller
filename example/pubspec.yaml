name: example
description: Example for async_builder.
publish_to: none

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  async_controller:
    path: ../
  intl: ^0.17.0
  faker: ^2.0.0-rc
  translator:
  provider: ^5.0.0
  flutter_hooks:


dev_dependencies:
  flutter_test:
    sdk: flutter
  lint: any

flutter:
  uses-material-design: true