name: example
description: Example for async_builder.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  async_controller:
  intl: ^0.15.8
  faker: ^1.1.1
  translator:

dependency_overrides:
  async_controller:
    path: ../ 

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true